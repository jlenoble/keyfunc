#### Hint type `'ignore'` !heading

The `'ignore'` hint is used whenever not to take into account a specific argument in the generated key function.

#include "build/docs/examples/type-ignore.test.md"
