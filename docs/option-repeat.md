#### Option `repeat` !heading

Option `repeat` allows to have all remaining arguments share the same hint. Only the last hint may have that option.

#include "build/docs/examples/option-repeat.test.md"
